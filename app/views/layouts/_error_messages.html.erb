<% if obj.errors.any? %>
  <div class="text-info p-0">
    <ul>
      <% obj.errors.full_messages_for(attribute).each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>